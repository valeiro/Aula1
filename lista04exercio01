#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <conio.h>
#include <string.h>

int main()
{
    char nome [30];
    int x,t, ca=0;
    printf("\n\t INFORME UM NOME: ");
    gets(nome);
    ca=strlen (nome);

    for (x=0; x <= t-0; x++)
    {
        if (nome[x]== 'a' || nome == 'A')
            ca++;
    }
    printf("\ O nome %s ", (nome));
    printf("tem %d letra a. ", ca);
    printf("\n \n");
    system("pause");
    return 0;
}
